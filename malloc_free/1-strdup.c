#include "main.h"
#include <stdlib.h>

/**
 * create_array - Create Array
 * @str: arg - size of array
 * Return: char *
*/
char *_strdup(char *str)
{
	int *p;
	
	if (size == 0)
	{
		return (NULL);
	}

	p = malloc(sizeof(c) * size);
	return (p);
}
